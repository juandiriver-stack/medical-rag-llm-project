version: "3.9"

services:
  api:
    build: .
    container_name: medical_rag_api
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - .:/app
    working_dir: /app
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
    restart: unless-stopped
    environment:
      - PYTHONUNBUFFERED=1
